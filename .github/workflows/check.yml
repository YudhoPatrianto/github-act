name: CI Build - Push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:          
      - name: Check Space 
        run: |
          df -h
